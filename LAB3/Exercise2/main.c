#include <stdio.h>
#include <stdlib.h>

int main()
{
    int A, B, C;
    A = 4;
    B = 5;
    C = (A<=B); // The result is one because the expression is true this expression can also be used with different operators such as !=, <= or >=
    return 0;
}
